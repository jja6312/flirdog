# Server Port
server:
  port: 8080

spring:
  profiles:
    default: local

  jpa:
    hibernate:
      ddl-auto: none

---
spring:
  config:
    activate:
      on-profile: local
  # 아래 이미지크기제한 널널하게 잡음. - 정지안, 11/29
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://db-k2g2a.vpc-cdb.ntruss.com:3306/studydb?serverTimezone=Asia/Seoul
    username: study
    password: bitcamp!@#123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true # (2.5~) Hibernate 초기화 이후 data.sql 실행
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
---
spring:
  kafka:
    consumer:
      bootstrap-servers: 10.0.3.16:9092
    producer:
      bootstrap-servers: 10.0.3.16:9092
---
spring:
  data:
    mongodb:
      host: k2g36.vpc.mg.naverncp.com
      port: 17017
      database: test
      username: user
      password: qwe123!@#
